---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crucix-runs
  namespace: observability
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
