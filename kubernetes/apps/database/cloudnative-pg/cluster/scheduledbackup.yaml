apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres18-backup
  namespace: database
spec:
  schedule: "0 0 0 * * *"
  suspend: false
  immediate: false
  backupOwnerReference: self
  cluster:
    name: postgres18
