---
# yaml-language-server: $schema=https://crd.movishell.pl/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app authentik
  namespace: security
spec:
  releaseName: authentik
  chart:
    spec:
      chart: authentik
      version: "2025.8.6"
      sourceRef:
        kind: HelmRepository
        name: goauthentik
        namespace: flux-system
  interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  values:
    global:
      deploymentAnnotations:
        reloader.stakater.com/auto: "true"

      envFrom:
        - secretRef:
            name: authentik-secret

      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: authentik-media

      volumeMounts:
        - name: media
          mountPath: /media

    # Authentik configuration
    authentik:
      # Use existing secret instead of creating one
      enabled: false
      existingSecret:
        secretName: authentik-secret

      log_level: info
      error_reporting:
        enabled: false

      # Database config (actual values come from secret via env vars)
      postgresql:
        host: "postgres18-rw.database.svc.cluster.local"
        port: 5432
        name: "authentik"
        user: "authentik"

    # Server configuration
    server:
      replicas: 1

      # Init container to ensure database exists
      initContainers:
        - name: init-db
          image: ghcr.io/home-operations/postgres-init:18.3@sha256:6fa1f331cddd2eb0b6afa7b8d3685c864127a81ab01c3d9400bc3ff5263a51cf
          envFrom:
            - secretRef:
                name: authentik-secret

      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 1536Mi

      # Enable metrics for monitoring
      metrics:
        enabled: true
        serviceMonitor:
          enabled: false

      # Use standard Kubernetes Ingress (Traefik will handle it)
      ingress:
        enabled: true
        ingressClassName: traefik
        hosts:
          - "auth.${SECRET_EXTERNAL_DOMAIN}"
        paths:
          - /
        pathType: Prefix
        tls:
          - secretName: authentik-tls
            hosts:
              - "auth.${SECRET_EXTERNAL_DOMAIN}"

      service:
        type: ClusterIP

    # Worker configuration
    worker:
      replicas: 1

      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 1536Mi

      metrics:
        enabled: true
        serviceMonitor:
          enabled: false

    # Redis configuration (bundled)
    redis:
      enabled: false

    # Disable bundled PostgreSQL (using external postgres18)
    postgresql:
      enabled: false

    # Prometheus rules
    prometheus:
      rules:
        enabled: false
